**Assignemnt 5 -Task 1**
student_marks = {...} => used to define a dictionary where the values are written inside {} and student_marks is the name of the dictionary.
input() => used to take user input and store it in variable "name".
if name in student_marks => checks if the input name is present in the defined dictionary.
f" " => embeds variables direclty into string using {}.

**Assignment 5 - Task 2**
range(...) => creates a sequence of numbers bteween the 2 numbers specified within ( ).
range(1,11) => creates a sequence of numbers between 1 and 11.
list() => converts the range into Python's list format so it can be manipulated.
: => used for slicing - cuts the list values till the specified value. 
[:5] => start of the list to the 5th index.
[::-1] => reads the list backwards - used to reverse a list.
